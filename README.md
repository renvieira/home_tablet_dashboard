There is an API key exposed in this project. The API key is currently hardcoded in the repository, which is not a best practice and should be considered insecure.
This is temporary and will be replaced by a server-side API Route and environment variables. The exposed key will be revoked and rotated as part of that change.
